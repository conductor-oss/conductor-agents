{
  "name": "pr_submit_review",
  "description": "Post a formal PR review (inline comments + summary + verdict) via the reviews API. Never APPROVEs. Retry 0 (re-post would duplicate). Input: repo, number, structured.",
  "retryCount": 0,
  "retryLogic": "FIXED",
  "retryDelaySeconds": 5,
  "timeoutSeconds": 86400,
  "responseTimeoutSeconds": 86400,
  "timeoutPolicy": "ALERT_ONLY",
  "ownerEmail": "viren@orkes.io",
  "pollTimeoutSeconds": 60
}
