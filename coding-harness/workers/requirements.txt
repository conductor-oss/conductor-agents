conductor-python==1.3.11
claude-agent-sdk>=0.2.110
openai-codex==0.1.0b3
# openai-codex's own pinned cli-bin dep, surfaced as a direct requirement so
# `uv pip install --prerelease=explicit` honors this pre-release (uv only allows
# pre-releases for packages with a direct explicit pre-release marker; keep in
# sync with the version openai-codex==0.1.0b3 requires).
openai-codex-cli-bin==0.137.0a4
